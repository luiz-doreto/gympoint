import styled from 'styled-components';

export const Container = styled.button`
    border: 0;
    background: none;
    margin-left: 10px;
    font-size: 15px;
    color: ${props => props.color};
`;
